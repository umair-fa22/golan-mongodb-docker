# Example .env for golan-mongodb-docker
# Copy this to .env and set real values (don't commit secrets)

# MongoDB connection URI (required)
# For a local MongoDB container: mongodb://mongo:27017
MONGODB_URI="mongodb://mongo:27017"

# Optional: application port
PORT=8080
